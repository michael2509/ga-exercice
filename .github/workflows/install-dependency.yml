name: Job's default

on:
  push:

jobs:
  job-default:
    defaults:
        run:
          working-directory: ./node-app
      runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Install dependencies
        run: npm install
      - name: Run tests
        run: npm test